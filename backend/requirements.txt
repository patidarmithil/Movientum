# ================================================================
# Movientum Backend — Python Dependencies
# Python 3.13 compatible (prebuilt wheels — no MSVC required)
# Install: pip install -r requirements.txt
# ================================================================

# --- Web Framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
gunicorn==23.0.0

# --- Database (Async SQLAlchemy + Supabase/PostgreSQL) ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0              # Python 3.13 wheels available since 0.30.0
psycopg2-binary==2.9.10      # Python 3.13 wheels available since 2.9.10
alembic==1.14.0              # schema migrations

# --- Cache (Redis / Upstash) ---
redis==5.2.0                 # pure Python fallback if hiredis unavailable

# --- Authentication ---
python-jose[cryptography]==3.3.0   # JWT creation + validation
passlib[bcrypt]==1.7.4             # bcrypt password hashing

# --- Configuration ---
python-dotenv==1.0.1
pydantic==2.11.7             # >=2.11.7 required by Supabase realtime/storage3
pydantic-settings==2.7.0     # Python 3.13 compatible

# --- HTTP Client (TMDB API calls) ---
httpx==0.27.2

# --- Background Tasks ---
celery[redis]==5.4.0

# --- YAML Config ---
pyyaml==6.0.2

# --- Utilities ---
python-multipart==0.0.12     # form data parsing
email-validator==2.2.0       # email format validation

# --- Dev / Testing ---
pytest==8.3.4
pytest-asyncio==0.24.0

#Testing comment
